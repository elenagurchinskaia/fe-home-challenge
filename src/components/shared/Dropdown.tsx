// reusable dropdown for priorities

// 1. accept props: 'options', 'selected', 'onChange'
// 2. render a 'select' element with the 'options' array
// 3. bind the 'onChange' event to the parent's handler
