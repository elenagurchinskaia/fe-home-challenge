// reusable input field component for text entry (both adding and editing tasks)

// 1. accept props: 'value', 'onChange'
// 2. render an input element of type 'text'
// 3. bind the input's 'onChange' event to the parent's handler
