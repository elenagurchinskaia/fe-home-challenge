// reusable checkbox component for toggling task completion

// 1. accept props: 'checked', 'onChange'
// 2. render an input element of type 'checkbox'
// 3. bind the checkbox's 'onChange' event to the parent's 'onChange' handler
