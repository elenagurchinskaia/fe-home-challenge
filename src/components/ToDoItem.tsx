// renders an individual task

// 1. import libraries and components (Checkbox, IconButton)
// 2. accept props: 'task', 'toggleTaskCompletion', 'editTask', 'deleteTask'
// 3. render task details (name, priority, completed status)
// 4. render a 'Checkbox' component to toggle task completion status
// 5. render a delete button using 'IconButton' components - bind on click event to 'deleteTask' function
// 6. make task name editable:
//  - if in edit mode, render a text input ('TextInput'), else display the task name
// 7. add logic to handle task completion toggle and deletion
