import "./App.css";
// 0. imposr libraries and components (useState, ToDoList, AddTask, etc.)
// 1. define 'tasks' state with properties: name, priority, and completed status
// 2. define 'addTask(name, priority)' to create a new task
// 3. define editTask(id, newName) to edit an existing task
// 4. define 'deleteTask (id)' to remove a task from the list
// 5. define 'toggleTask Completion(id)' to mark tasks as completed or not
// 6. render the following components"
//  - 'ToDoList' passing the tasks list and functionns for toggling completion, edit and deletion
//  - 'AddTask' passing the addTask function

function App() {
  return <h1 className="text-3xl font-bold">Hello world!</h1>;
}

export default App;
